/**
 * BİLEŞEN: Sol kenar çubuğu sarmalayıcısı — sadece AppMenu'yü render eder.
 */

import AppMenu from './AppMenu';

const AppSidebar = () => {
    return <AppMenu />;
};

export default AppSidebar;
